#%%
# get the min and max of all data
# import numpy as np
# import os

# path = "data/PM25_hourly_sample_by_station/"
# names = os.listdir(path)
# arr = np.load("".join([path, names[0]]), allow_pickle=True)
# Max, Min = arr.max(), arr.min()

# for name in names:
#     arr = np.load("".join([path, name]), allow_pickle=True)
#     print(arr.max(), arr.min())
#     Max, Min = np.max([Max, arr.max()]), np.min([Min, arr.min()])

# print(Min, Max)

# %%
O3_min = 0.0
O3_max = 0.2475029081106186
PM25_min = 0.0
PM25_max = 422.754064293465

# %%
